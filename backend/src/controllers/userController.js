const userModel = require('../models/userModel');

const user_api_key = async (req, res) => {

    // procurar la coincidencia con el usuario y sus claves, el accesso a la base de datos de la table
    // users, debe hacerse con una funcion del userModel
};




module.exports = {
    user_api_key
};